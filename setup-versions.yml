# After Effects setup image versions for Shotwright.
# Modifying this file triggers the ae-setup-publish workflow which downloads
# the AE installer payload, patches the helper Setup.exe, and publishes
# a container image to GHCR.

image-repository: "ghcr.io/liuchangfreeman/shotwright/after-effects-setup"
current: "26.2"

versions:
  "26.2":
    product-id: AEFT
    platform: win64
    install-dir-name: "Adobe After Effects 2026"
