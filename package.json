{
  "name": "shotwright",
  "version": "0.2.0",
  "description": "Container-first Adobe After Effects runtime for AI agents",
  "license": "MIT",
  "scripts": {
    "validate": "powershell -ExecutionPolicy Bypass -File .\\scripts\\validate\\run_validation.ps1"
  },
  "engines": {
    "node": ">=20"
  }
}
